# Generated results

Pipeline outputs are written here by default and are ignored by Git. This conservative default prevents accidental publication of subject-level predictions, fold assignments, ancestry profiles, or other restricted derivatives.

Before publishing any result, copy only governance-approved aggregate tables and final figures into a separately reviewed release directory and rerun the release checker.

